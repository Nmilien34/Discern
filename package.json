{
  "name": "discern",
  "version": "0.1.0",
  "private": true,
  "description": "Discern \u2014 scripture, discernment, and a cultivation path",
  "workspaces": [
    "shared",
    "discern-backend"
  ],
  "scripts": {
    "predev": "npm run build -w @discern/shared",
    "dev": "concurrently -n shared,api -c cyan,green \"npm run dev -w @discern/shared\" \"npm run dev -w @discern/backend\"",
    "build": "npm run build -ws --if-present",
    "prelint": "npm run build -w @discern/shared",
    "lint": "eslint .",
    "pretypecheck": "npm run build -w @discern/shared",
    "typecheck": "npm run typecheck -ws --if-present",
    "test": "npm run test -ws --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.25.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.30.1"
  }
}
